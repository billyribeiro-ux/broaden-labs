node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results

# Playwright
playwright-report
blob-report

# Lighthouse CI reports are build artifacts, not source.
.lighthouseci

# pnpm store, created inside the project by a containerised install.
.pnpm-store
# Added by `vercel link`, then narrowed. Bare `.env*` also swallows
# .env.example, which is the file the README tells you to copy — and an ignored
# .env.example silently stops being maintained.
.env
.env.*
!.env.example
.env*
